NAME=myshortname
	# the short name to appear on registrar
DXAHOST=$(hostname)
DXAPORT=9443
	# a machine name and port the Directory Manager can resolve
	# and connect to the dxagent
DXACLIENT="dxagent"
	# client name when installing the dxagent
