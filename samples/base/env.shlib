TEST=
	# set a non-empty value for testing purpose
ENVNAME=$BASEENVNAME${TEST}
DXAGENT=$BASEDXAGENT${TEST}
DXACLIENT="dxagent"
DXAHOST=$(hostname)
	# DXAHOST needs to be set to a machine name
	# the Directory Managers that are going to manage the
	# dxagent can resolve and connect to.
	# This base sample is best run on the Directory machine
	# right after the installation of the Directory Manager,
	# dxagent, and the dxserver on the same machine.
