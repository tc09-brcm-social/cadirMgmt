PREFIX="ou=sessionstore,o=SiteMinder"
	# set your own prefix for the DSA
DBSIZE=
	# set the database size you need,
	# The default data size is rather small
JSONDIR=../defdata
	# the subdirectory that holds a sample JSON payload
CACHEINDEX='["smSessionId", "smExpirationTime", "smIdleExpirationTime", "smSearchData", "smVariableName", "smFullVariableName"]'
DSAFLAGS='["multi-write-async", "no-service-while-recovering"]'
